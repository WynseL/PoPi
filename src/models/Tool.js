class Tool {
  constructor(id, prefix, type, recipe, usesCount) {
    this.id = id;
    this.prefix = prefix;
    this.type = type;
    this.recipe = recipe;
    this.usesCount = usesCount;
  }
}

export default Tool;
